name: Agent Reconcile PR101 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Disabled one-time workflow
        run: echo "One-time PR101 repair completed; this workflow is intentionally disabled and may be removed."
