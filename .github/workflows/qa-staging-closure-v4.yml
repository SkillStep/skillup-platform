name: QA Final Staging Closure V4 (retired)

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Retired; V5 is the active staging closure with bounded SSH retry.'
