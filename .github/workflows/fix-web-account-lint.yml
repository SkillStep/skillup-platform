name: Fix web account lint

on:
  push:
    branches:
      - feat/20-web-sign-in-onboarding

permissions:
  contents: write

concurrency:
  group: fix-web-account-lint
  cancel-in-progress: false

jobs:
  fix:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: feat/20-web-sign-in-onboarding
          persist-credentials: true

      - name: Apply reviewed Biome fixes
        shell: bash
        run: |
          set -euo pipefail
          corepack enable
          corepack prepare pnpm@11.17.0 --activate
          pnpm install --frozen-lockfile
          pnpm exec biome lint . --write --unsafe
          pnpm format
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          if ! git diff --cached --quiet; then
            git commit -m "style: apply account-flow lint fixes"
            git push origin HEAD:feat/20-web-sign-in-onboarding
          fi
